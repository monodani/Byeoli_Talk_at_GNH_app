# ================================================================
# BYEOLI_TALK_AT_GNH_app - 완전한 requirements.txt
# 경상남도인재개발원 RAG 챗봇의 모든 의존성 라이브러리
# ================================================================

rank-bm25

# --- 🔧 Core Framework & Configuration ---
python-dotenv==1.0.1          # .env 파일 로드
pydantic==2.8.2               # 데이터 검증 및 타입 체크 (contracts.py)
structlog==24.4.0             # 구조화된 로깅
loguru==0.7.2                 # 고급 로깅 유틸리티

# --- 📊 Data Processing & Analysis ---
pandas==2.2.2                 # CSV 데이터 처리 (만족도 조사, 연락처)
numpy==1.26.4                 # 수치 연산 (벡터 연산, 통계)
rapidfuzz==3.9.6              # 빠른 문자열 매칭 (엔티티 추출)
openpyxl==3.1.5               # Excel 파일 처리 (선택적)

# --- 🔍 Search & Retrieval ---
rank-bm25==0.2.2              # BM25 키워드 검색
faiss-cpu==1.8.0.post1        # 벡터 유사도 검색 (CPU 버전)
scikit-learn==1.5.1           # MMR 및 클러스터링

# --- 📄 File Processing & Text Extraction ---
pypdf==5.1.0                  # PDF 텍스트 추출 (2025plan.pdf, 2024pyeongga.pdf)
PyPDF2==3.0.1                 # PDF 처리 보조 라이브러리
Pillow==10.4.0                # 이미지 처리 (식단표 이미지)
pytesseract==0.3.13           # OCR 텍스트 인식 (requires tesseract binary)
tiktoken==0.7.0               # OpenAI 토큰 계산 (청킹 최적화)
chardet==5.2.0                # 인코딩 감지 (CSV 파일)

# --- 🤖 AI & Machine Learning ---
openai==1.37.1                # OpenAI API (GPT-4o-mini, text-embedding-3-large)
langchain==0.2.12             # LangChain 프레임워크
langchain-openai==0.1.20      # LangChain OpenAI 통합
langchain-community==0.2.11   # LangChain 커뮤니티 확장
sentence-transformers==3.0.1  # 로컬 임베딩 모델 (백업용)

# --- 🌐 Web Framework & API ---
streamlit==1.38.0             # 웹 UI 프레임워크
requests==2.32.3              # HTTP 요청 (ChatGPT API 호출)
uvicorn==0.30.5               # ASGI 서버 (선택적 API 서빙)
fastapi==0.112.0              # API 프레임워크 (확장용)
httpx==0.27.0                 # 비동기 HTTP 클라이언트

# --- ✅ Validation & Schema ---
jsonschema==4.21.1            # JSON 스키마 검증 (schemas/*.json)
marshmallow==3.21.3           # 데이터 직렬화/역직렬화
email-validator==2.2.0        # 이메일 형식 검증

# --- ⚡ Performance & Async ---
asyncio-throttle==1.0.2       # 비동기 작업 제한
tenacity==8.3.0               # 재시도 로직 (API 호출 안정성)
cachetools==5.4.0             # 메모리 캐시
diskcache==5.6.3              # 디스크 캐시
aiofiles==24.1.0              # 비동기 파일 I/O

# --- 🔄 File Monitoring & Hotswap ---
watchdog==4.0.0               # 파일 변경 감지 (핫스왑)
filelock==3.15.4              # 파일 잠금 (동시성 제어)

# --- 📈 Monitoring & Observability ---
psutil==6.0.0                 # 시스템 리소스 모니터링
memory-profiler==0.61.0       # 메모리 사용량 프로파일링

# --- 🔐 Security & Authentication ---
cryptography==42.0.8          # 암호화 (민감정보 보호)
bcrypt==4.1.3                 # 패스워드 해싱

# --- 📦 Serialization & Data Format ---
orjson==3.10.3                # 고성능 JSON 처리 (로그, 캐시)
msgpack==1.0.8                # 바이너리 직렬화
pickle-secure          # 안전한 피클링

# --- 🌏 Internationalization ---
langdetect==1.0.9             # 언어 감지
koala==0.1.0                  # 한국어 처리 (선택적)

# --- 🔧 Development & Testing ---
pytest==8.3.2                 # 테스트 프레임워크
pytest-cov==5.0.0            # 코드 커버리지
pytest-asyncio==0.23.8        # 비동기 테스트
pytest-mock==3.14.0           # 모킹 라이브러리
black==24.4.2                 # 코드 포맷팅
ruff==0.5.7                   # 린팅 및 포맷팅
mypy==1.11.1                  # 타입 체킹
pre-commit==3.7.1             # Git 훅

# --- 📊 Data Visualization (Optional) ---
matplotlib==3.9.1             # 기본 차트
seaborn==0.13.2               # 통계 시각화
plotly==5.22.0                # 인터랙티브 차트

# --- 🔗 Database & Storage (Optional) ---
sqlalchemy==2.0.31            # ORM (향후 확장용)
redis==5.0.8                  # 캐시 저장소 (분산 환경용)

# --- 🌐 Network & Communication ---
websockets==12.0              # 실시간 통신 (향후 확장)
pydantic-settings==2.3.4      # 설정 관리

# --- 📝 Text Processing & NLP ---
nltk==3.8.1                   # 자연어 처리 도구
konlpy==0.6.0                 # 한국어 형태소 분석 (선택적, requires JDK)
soynlp==0.0.493               # 한국어 처리 (KoNLPy 대안)
regex==2024.5.15              # 고급 정규표현식

# --- 🔄 Task Scheduling & Background Jobs ---
apscheduler==3.10.4           # 스케줄링 (주기적 인덱스 업데이트)
celery==5.3.6                 # 백그라운드 작업 (대규모 환경용)

# --- 🧮 Math & Statistics ---
scipy==1.13.1                 # 과학 계산 (통계 분석)
statsmodels==0.14.2           # 통계 모델링

# --- 🔍 Debugging & Profiling ---
line-profiler    		# 라인별 성능 프로파일링
py-spy==0.3.14                # 런타임 프로파일러
ipdb==0.13.13                 # 향상된 디버거

# --- 🎨 Rich CLI & UI ---
rich==13.7.1                  # 콘솔 출력 향상
click==8.1.7                  # CLI 인터페이스
typer==0.12.3                 # 현대적 CLI 프레임워크

# --- 🔧 System & Platform ---
python-multipart==0.0.9       # 멀티파트 폼 데이터
typing-extensions==4.12.2     # 타입 힌트 확장
python-dateutil==2.9.0        # 날짜 처리
pytz==2024.1                  # 시간대 처리

# --- 📱 Mobile & Cross-platform (Future) ---
kivy==2.3.0                   # 모바일 앱 (향후 확장용)

# --- ⚙️ Configuration & Environment ---
configparser==7.0.0           # 설정 파일 파싱
pathlib2==2.3.7              # 향상된 경로 처리
platformdirs==4.2.2           # 플랫폼별 디렉터리

# --- 🌊 Streaming & Real-time ---
sseclient-py==1.8.0          # Server-Sent Events
asyncio-mqtt==0.16.2          # MQTT 클라이언트 (IoT 확장용)

# --- 🎯 Specific Domain Libraries ---
transformers==4.41.2          # Hugging Face 트랜스포머 (로컬 모델용)
torch==2.3.1                 # PyTorch (로컬 ML 모델용, CPU 버전)
torchvision==0.18.1          # 컴퓨터 비전
torchaudio==2.3.1            # 오디오 처리

# --- 📐 Geometry & Spatial ---
shapely==2.0.4                # 지리공간 데이터 (향후 지도 기능용)

# --- 🔄 Version Control Integration ---
gitpython==3.1.43             # Git 통합

# --- 🌈 Color & Terminal ---
colorama==0.4.6               # 터미널 색상 (Windows 호환)
termcolor==2.4.0              # 터미널 색상

# ================================================================
# 선택적 의존성 (환경에 따라 설치)
# ================================================================

# --- GPU 가속 (CUDA 환경) ---
# faiss-gpu==1.8.0.post1      # FAISS GPU 버전 (CUDA 필요)
# torch==2.3.1+cu118          # PyTorch GPU 버전

# --- 한국어 NLP (JDK 필요) ---
# konlpy==0.6.0               # KoNLPy (Java 의존성)
# mecab-python3==1.0.8        # MeCab (시스템 설치 필요)

# --- 데이터베이스 드라이버 ---
# psycopg2-binary==2.9.9      # PostgreSQL
# pymongo==4.8.0              # MongoDB

# --- 클라우드 스토리지 ---
# boto3==1.34.144             # AWS S3
# google-cloud-storage==2.17.0 # Google Cloud Storage

# ================================================================
# 시스템 요구사항 (별도 설치 필요)
# ================================================================
# - tesseract-ocr (OCR 엔진)
# - poppler-utils (PDF 처리)
# - ffmpeg (멀티미디어 처리, 선택적)
# - redis-server (분산 캐시, 선택적)
# - java 8+ (KoNLPy 사용 시)

# ================================================================
# 설치 순서
# ================================================================
# 1. pip install --upgrade pip setuptools wheel
# 2. pip install -r requirements.txt
# 3. 시스템 패키지 설치:
#    - Ubuntu: sudo apt-get install tesseract-ocr tesseract-ocr-kor poppler-utils
#    - macOS: brew install tesseract tesseract-lang poppler
#    - Windows: 별도 바이너리 설치 필요

# ================================================================
# 버전 고정 이유
# ================================================================
# - 프로덕션 환경에서 일관성 보장
# - 의존성 충돌 방지
# - 보안 취약점 추적 용이성
# - 재현 가능한 빌드 보장
